## 1.1.4 (2026-06-27)

### 新增

**划词翻译**
- 新增跨平台的原生选区文本获取策略，Windows 会优先尝试 UI Automation，再回退到稳定后的剪贴板复制流程，减少快捷键修饰键和焦点时序导致的失败。

### 优化

**翻译窗口**
- 翻译窗口会根据工作流文本自适应大小，同时保留用户记住的位置和手动调整后的尺寸。
- 改进富文本翻译结果渲染、缺失服务提示、图片 OCR 防护和异常反馈，让失败状态更清晰。

**更新体验**
- 更新窗口和“关于”页的状态与进度摘要更紧凑，发布说明区域更易阅读。

**安装包体积**
- Windows 安装包只保留 `en-US` 和 `zh-CN` Electron 语言包，并将完整 flag-icons 样式替换为语言菜单按需使用的 SVG 资源。

### 修复

**快捷键**
- 统一设置页和插件热键编辑器的 Electron accelerator 映射，修复小键盘运算符和部分特殊键生成无效快捷键的问题。

**设置与插件**
- 恢复通过 `/plugin` 直接进入设置页插件管理的能力。
- 修复语言菜单中被 Vite 内联的小体积国旗 SVG 不显示的问题。

**构建与配置**
- 修复生产构建使用的 Tailwind 配置路径。
- 加强代理主机校验，避免无效代理配置进入运行时请求。

### 安全与维护

- 升级 `react-router`、`lodash`、`vite` / `esbuild` 和传递依赖 `undici`，处理已知 Dependabot 告警。
- 加强标签发布校验，CI 会拒绝包版本、changelog 版本和 React Hooks 依赖告警不一致的发布。
