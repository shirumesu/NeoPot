## 1.1.0 (2026-6-5)

### 新增功能

**更新系统**
- 实现完整的更新分发系统：使用 electron-vite + electron-builder 构建 Windows NSIS 安装包和便携版，支持自动更新检测和手动检查更新，在检测到新版本时显示右下角更新提醒窗口

**插件系统**
- 实现完整的插件系统：支持从 GitHub 仓库、JSON URL、本地文件或文件夹安装 `.zip` 格式的 JavaScript 插件
- 新增插件管理界面，包括已安装插件列表和插件市场，支持启用/禁用、卸载、查看主页、配置独立快捷键
- 插件可以提供翻译、OCR、TTS、语言检测服务
- 插件市场支持自定义来源，可添加多个插件源，区分远程下载源和开发备用源
- Windows 下插件安装提供两个独立按钮：文件选择器（.zip 压缩包）和文件夹选择器（插件目录）
- 内置 Lingva 翻译插件作为示例
- 从主仓库移除打包插件相关命令和目录，插件分发转移到 `Neopot-releases` 仓库

**日志系统**
- 迁移到 `electron-log` 日志系统，支持配置日志级别（调试/信息/警告/错误/静止）
- 在通用设置中新增日志级别下拉菜单，可以实时切换日志级别
- 开发模式默认为调试级别，打包后默认为信息级别

**国际化**
- 完整的中英文界面翻译，覆盖所有设置页面、窗口和托盘菜单
- 自动从语言文件目录发现可用的语言包，支持多语言回退机制

**配置管理**
- 实现跨窗口配置同步：当一个窗口修改设置后，其他打开的窗口会自动刷新显示最新配置
- 优化配置保存反馈：显示保存成功/失败的提示信息，导航和未更改的服务列表重新排序不再显示保存提示
- 敏感配置字段（如 API 密钥）使用 Electron `safeStorage` 加密存储
- 添加"关闭到托盘"设置，默认启用：配置窗口关闭时隐藏到托盘，托盘退出/重启仍会退出应用

**服务提供商**
- 保留 Google、DeepL、Ollama、本地模型作为内置翻译和 OCR 服务
- 移除内置 TTS 服务、历史记录、备份、收藏、透明设置等功能
- TTS 现在完全通过插件提供
- 网络请求通过 Electron `net.fetch` 路由，自动应用系统代理设置

### 架构迁移

**Electron 迁移**
- 完成从 Tauri 到 Electron 的完整迁移，移除 `src-tauri/` 目录
- 实现无边框/可调整大小窗口、翻译窗口光标定位、窗口事件传递、重复选中文本增量追加、截图完成路由
- 实现快捷键、选中文本输入、语言检测、字体列表、托盘刷新、图像复制、代理切换等桌面命令兼容性
- 完成 Windows Squirrel 打包，包括安装/更新/卸载快捷方式事件处理、图标资源、元数据设置
- 实现打包后的原生数据库支持，包括 `better-sqlite3` 重建和 `.node` 绑定解包
- 使用自定义 `neopot://` 协议服务打包后的渲染进程，修复打包版本中 PaddleOCR 无法使用的问题
- 窗口生命周期事件从 `tauri://*` 迁移到 `neopot://*`

**安全性增强**
- 打包的窗口忽略 `ELECTRON_RENDERER_URL` 环境变量
- preload 仅向受信任的渲染器来源暴露 `window.neoPot` API
- 文件系统 IPC 限制在应用数据目录内，删除操作仅限于 `AppCache`
- 插件代码在沙盒 iframe 中运行，通过显式 RPC 调用工具函数
- 渲染器 HTTP 阻止通用私有网络目标和不安全重定向
- 外部 URL 使用安全辅助函数打开
- 更新检查忽略非语义化版本的发布标签

### 改进优化

**用户界面**
- 优化关于页面：网站、社区、邮箱链接改为内联气泡提示
- 优化服务设置布局：DeepL 服务类型选择器调整为半宽右栏控件
- 移除界面中残留的 Pot 帮助链接和名称
- 剪贴板监控开关从通用设置移至翻译设置

**性能优化**
- 减少设置页面保存延迟：服务器端口输入改为失焦提交，跳过默认的保存后验证往返

**系统兼容性**
- macOS 支持复制选中文本，并安全恢复剪贴板内容
- 稳定 Windows/Squirrel 自动启动功能，使用父级启动器而非版本化可执行文件路径
- Linux 选择翻译使用通用辅助回退方案

### 问题修复

**核心功能**
- 修复快捷键编辑仅在确认时持久化，快捷键清除可以持久化
- 修复识别/截图窗口的非翻译工作流事件不再被渲染器就绪门控阻止

**翻译和识别**
- 修复 OCR 识别文本翻译交接
- 修复未更改输入的手动重新翻译
- 修复通过提供商调用的显式语言传播
- 修复剪贴板监控从配置启动并响应 `clipboard_monitor` 设置
- 修复语言检测默认为本地/插件选择

**其他修复**
- 修复 DeePL 非字符串 `authKey` 崩溃，转换为清晰的配置错误
- 修复截图捕获使用主显示元数据和主侧缩放因子裁剪
- 修复 electron-vite 主依赖外部化，使 `electron-store@11` 被打包而非通过 CJS `require` 加载
- 修复打包前清理生成的发布输出，收窄 electron-builder 打包文件范围
- 应用 ESLint 和 Tailwind CSS 修复到服务、窗口和工具模块

### 其他变更
- 简化包脚本为 `dev`、`build`、`make`、`release`、`lint`、`format`、`format:check`、`test`
- 移除过时的 Tauri/updater/build 别名、陈旧的发布工作流/actions 和旧的更新器辅助脚本
- 打包后的 NSIS 和便携版安装包约 126 MB，`app.asar` 约 80 MB，解包后约 424 MB
